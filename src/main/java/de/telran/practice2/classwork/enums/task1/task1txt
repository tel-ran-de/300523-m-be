Задача 1.

1. Понять, как можно посмотреть методы, которым обладает любой Enum? Почему эти методы доступны в любом созданном нами enum
по умолчанию

2.Создать enum для времен года, такой, что бы помимо самого enum у времени года было строковое
описание.

3. Написать такой код, который:
 - распечатывает все значения енама
 - распечатывает все строковые описания каждого енама
 - распечатывает значение любого енама через обращение через название енам
 - распечатывает значение енам при помощи передачи в метод строкового описания времени года
 - распечатывает порядковый индекс любого времени года в классе енама
